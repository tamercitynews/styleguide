<div class="module most-read theme-border">
	<h5 class="module-title">I Più Letti</h5>
	<ul class="u-unstyled u-bordered">
    {% for story in range(0,4) %}
      {# {% set loopStart = loop.index %}
      {% set loopSlice = 5 %}
      {% if loopStart <= loopSlice %} #}
        <li>
          {% render "@story-list-orderedv2015" %}
        </li>
      {# {% endif %} #}
    {% endfor %}
	</ul>
</div>
